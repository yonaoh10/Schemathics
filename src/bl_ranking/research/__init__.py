"""Vendored, unmodified research code. See README.md in this directory.

Importing either module here executes `NameDataset()` at module scope (18.6 s, 2.4 GB),
so production code imports them lazily and only where they are actually needed.
"""
